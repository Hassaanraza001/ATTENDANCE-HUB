
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // FULL KHULI CHOOTH: Allowing all reads and writes to all collections
    // This ensures Kiosk, Dashboard, and Raspberry Pi can sync without permission errors.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
